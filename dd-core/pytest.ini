[pytest]
# Makes dd-core self-rooting: wherever this copy is vendored, pytest uses THIS
# as the rootdir instead of walking up into a host project's config (which may
# ignore or misconfigure these tests). Without it, `pytest tests/` from a
# vendored copy collected 0 items because the host repo's conftest ignored it.
testpaths = tests
